This folder holds UI screenshots referenced by `README.md` in the project root.

Expected filenames (used by README.md):

- `01_home.png` — Search home page
- `02_results_shakespeare.png` — Results page for query "shakespeare"
- `03_query_light.png` — Search query example
- `04_results_light.png` — Results page for query "light"

To add screenshots:

1. Copy PNG files into this folder and name them as above.
2. Commit and push:

```bash
git add zFlask/screenshots/*.png
git commit -m "Add UI screenshots"
git push origin feature/ir-pipeline
```

If you prefer, upload the PNGs here and I can add them for you.